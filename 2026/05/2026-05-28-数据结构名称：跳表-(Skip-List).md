# 数据结构名称：跳表 (Skip List)

# 原理说明：
# 跳表是一种概率性查找数据结构，它允许在一个有序链表的基础上，
# 插入多层“索引”链表，从而实现平均 O(log n) 的查找、插入和删除操作。
# 想象一下，你有一个单向链表，非常慢（O(n) 查找）。
# 为了加速，我们为它增加一些“快车道”。
#
# 核心思想：
# 1. 多层链表：跳表由多个层级的有序链表组成。
#    - 最底层 (Level 0) 包含所有元素，形成一个标准的有序链表。
#    - 越