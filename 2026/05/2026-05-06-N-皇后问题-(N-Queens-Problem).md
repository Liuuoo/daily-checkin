## N 皇后问题 (N-Queens Problem)

### 1. 题目描述

在 N×N 的国际象棋棋盘上放置 N 个皇后，使得任意两个皇后都不能互相攻击。这意味着：
*   每行有且仅有一个皇后。
*   每列有且仅有一个皇后。
*   每条对角线（包括主对角线和副对角线）上最多只能有一个皇后。

要求找出所有可能的 N 皇后放置方案。

**输入**: 一个整数 `n`，表示棋盘的大小和皇后的数量。
**输出**: 一个列表，其中每个元素是一个表示一个有效解决方案的列表。每个解决方案又是一个列表，包含 `n` 个字符串，每个字符串代表棋盘