```python
# -*- coding: utf-8 -*-

# --- 数据结构：Trie (前缀树) ---
#
# 原理说明:
# Trie（发音同 "try"），也称为前缀树或字典树，是一种用于高效检索字符串集合的数据结构。
# 它的主要思想是：
# 1.  每个节点代表一个字符串的某个前缀。
# 2.  根节点代表空字符串。
# 3.  从根节点到任意节点的路径，都代表一个字符串的前缀。
# 4.  路径上的每个边（或子节点）代表一个字符。
# 5.  通常，节点会有一个标记，用于指示该节点代表的路径是否构成一个完整的单词