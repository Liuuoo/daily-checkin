```python
import random
import math

# 1. 数据结构名称和原理说明 (用注释)
# Skip List (跳表)
#
# 原理:
# 跳表 (Skip List) 是一种概率性查找数据结构，它通过在链表上构建多层“快速通道”来提高查找、插入和删除的效率。
# 它的平均时间复杂度为 O(log n)，与平衡二叉搜索树（如红黑树）相当，但实现起来通常更简单。
#
# 核心思想:
# 1. 多层链表: 跳表由多层有序的链表组成。最底层（Level 0）包含所有元素，形成一个标准的有序链表。
#    